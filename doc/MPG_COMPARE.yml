pairs:
  - ours: intern/cycles/manifold/mpg_seed.cpp::mpg_build_seed
    ref:  extern/mpg_ref/src/.../seed.cpp::build_seed
  - ours: intern/cycles/manifold/mpg_solve.cpp::mpg_solve_connect
    ref:  extern/mpg_ref/src/.../solve.cpp::connect
  - ours: intern/cycles/manifold/mpg_pdf.cpp::mpg_compute_pdf
    ref:  extern/mpg_ref/src/.../pdf.cpp::compute_pdf
  - ours: intern/cycles/kernel/integrator/shade_surface.h::MPG block
    ref:  extern/mpg_ref/src/.../integrator.cpp::direct_mpg_sample
checklist:
  - When is the light pdf evaluated (before/after moving the light sample)?
  - Exact formula for technique pdf: p_mpg = p_seed * p_light(spec_pt) * J (receiver solid angle).
  - Which pdfs are in the MIS denominator (p_bsdf, p_light(NEE), p_mpg) and are they all same measure?
  - Where is the Jacobian applied (once, not twice)?
  - Are receiver labels singular or non-singular?
  - Is visibility/shadow test & accumulation identical to NEE?
